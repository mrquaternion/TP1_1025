public class HeroEquilibre extends Hero {
    
    // Constructeur
    public HeroEquilibre(int health, int maxHealth, int level, int experience) {
        super(health, maxHealth, level, experience);
    }
}